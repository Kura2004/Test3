using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mentos : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Oyasuminasai");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
